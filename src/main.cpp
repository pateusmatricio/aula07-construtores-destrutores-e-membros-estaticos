#include <iostream>
using std::cin;
using std::cout;
using std::endl;

int _percurso = 25;

#include "sapo.hpp"

int main(){

	sapo

	while(a.getDistanciaPercorrida < _percurso && b.getDistanciaPercorrida < _percurso && c.getDistanciaPercorrida < _percurso){
		Sapo * a = new Sapo;
		
	}
	
}
